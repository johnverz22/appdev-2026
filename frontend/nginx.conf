server {
    listen 3000;

    root /usr/share/nginx/html;
    index index.html;

    # Serve the React SPA — return index.html for any unknown path
    location / {
        try_files $uri $uri/ /index.html;
    }
}
